# Schema Inference vs Explicit Schemas

_Generated by `spark_jobs/infer_schemas.py --mode full` from the raw files in HDFS (log: `reports/processing_logs/infer_schemas_full_20260924_190343.log`)._

Spark can infer types (`inferSchema=true` for CSV, automatic for JSON). The table below shows where that guess differs from the explicit schemas in `spark_jobs/schemas.py`, and why. For each difference the raw values were re-checked against the explicit type; *failing values* is how many values really are invalid for the documented type.

| table | format | inference time (s) | columns | differences |
|---|---|---|---|---|
| stops | csv | 14.58 | 8 | 0 |
| routes | csv | 1.26 | 11 | 0 |
| route_stops | csv | 1.75 | 7 | 0 |
| service_calendar | json | 1.37 | 8 | 3 |
| schedules | csv | 3.47 | 11 | 2 |
| vehicles | csv | 1.13 | 10 | 0 |
| passengers | csv | 1.57 | 8 | 0 |
| trips | csv | 15.57 | 15 | 0 |
| passenger_counts | csv | 7.23 | 11 | 0 |
| tickets | csv | 13.06 | 13 | 1 |
| delays | csv | 5.19 | 12 | 1 |
| gps_events | jsonl | 9.62 | 10 | 1 |

## Differences

| table | column | explicit | inferred | failing values | reason |
|---|---|---|---|---|---|
| service_calendar | start_date | `date` | `string` | 0 | JSON has no date type (dates stay strings) |
| service_calendar | end_date | `date` | `string` | 0 | JSON has no date type (dates stay strings) |
| service_calendar | exceptions | `array<struct<date:date,exception_type:string,reason:string>>` | `array<struct<date:string,exception_type:string,reason:string>>` | 0 | nested dates inferred as strings and struct fields ordered alphabetically |
| schedules | start_time | `string` | `timestamp` | 0 | values look like dates but are stored as text by design |
| schedules | end_time | `string` | `timestamp` | 0 | values look like dates but are stored as text by design |
| tickets | entry_time | `timestamp` | `string` | 2,986 | 2,986 values are not valid timestamps (e.g. ['31/02/2026 10:00', 'N/A', '2025-09-31 08:00:00']), so Spark fell back to string |
| delays | delay_minutes | `double` | `string` | 666 | 666 non-numeric values (e.g. ['#VALUE!', 'unknown', 'n/a']) turned the whole column into string |
| gps_events | event_time | `timestamp` | `string` | 0 | JSON has no timestamp type: JSON inference never produces timestamps, only strings |

## Why the pipeline uses explicit schemas

- **Correct types even with dirty data:** a handful of bad values should become quarantined rows, not turn a whole column into strings.
- **No extra pass over the data:** inference reads the files once just to guess types (time above).
- **Stable contract:** the same types every run and in every mode, matching `documentation/schemas/`.
- **Identifiers stay text:** IDs and codes are never parsed as numbers.
